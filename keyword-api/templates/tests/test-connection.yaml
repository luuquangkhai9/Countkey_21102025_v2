apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keyword-api.fullname" . }}-test-connection"
  labels:
    {{- include "keyword-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keyword-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
