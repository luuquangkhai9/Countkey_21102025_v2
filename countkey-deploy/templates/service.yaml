apiVersion: v1
kind: Service
metadata:
  name: {{ include "countkey-deploy.fullname" . }}
  labels:
    {{- include "countkey-deploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "countkey-deploy.selectorLabels" . | nindent 4 }}
