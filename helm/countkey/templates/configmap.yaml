apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "countkey.fullname" . }}-config
  labels:
    {{- include "countkey.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
