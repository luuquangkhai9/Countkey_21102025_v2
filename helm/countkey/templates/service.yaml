apiVersion: v1
kind: Service
metadata:
  name: {{ include "countkey.fullname" . }}
  labels:
    {{- include "countkey.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "countkey.selectorLabels" . | nindent 4 }}
